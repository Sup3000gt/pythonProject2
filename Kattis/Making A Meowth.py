N, P, X, Y = map(int, input().split())  # map() function returns a map object(which is an iterator)
                                        # of the results after applying the given function to each item
                                        # of a given iterable (list, tuple etc.)
                                        # map(fun, iter)

# N = every Nth page Meowth read
# P = total page you read
# X = time for you to read a page
# Y = time for Meowth read a page


